'use client'

import ReplayBanner from './ReplayBanner'

/** Global replay chrome — banner on all dashboard routes when a scenario is loaded. */
export default function ReplayChrome() {
  return <ReplayBanner />
}
